# Research and runtime Git closeout — 2026-09-09

This closeout packages the completed local backlog without changing production
source, tests, dependencies or experiment evidence. It does not authorize a new
model experiment, five-year study, recurring service, Paper or Live execution.
The next information-coverage and research-workflow design is a separate task phase.

## Dependency-ordered commits

1. Frozen optional method catalog and native pi context accounting. The runtime
   foundation precedes its role consumers so intermediate imports remain complete.
2. Continuous research/account handoff: Research Thesis v2, Portfolio v6, candidate
   proofs, scoped Recall, shared account opportunity, source timing, recovery and
   complete-group budget admission. This includes method integration in the roles.
3. Staged preparation/execution and read-only retrospective/audit tools, with
   corresponding commands, examples and tests.
4. Research reports, public summary artifacts, method materials and claim limits.

The original 259 changed/new files were saved locally before staging. Source and
test contents are unchanged from that snapshot. Only this closeout record and the
roadmap's current-status summary were added during packaging. Private source data,
experiment stores, model weights and machine state are outside the commits.

## Verification and provenance

- The final full local Python run passed **2,245 tests** with 212 warnings in
  1,438.76 seconds. Its log SHA-256 is
  `9c889742278fed0d9b68ff62d6dfd765642cec30a61c0ffb6bde71da32a9ab63`.
  The result is retained in the [Skills verification summary](research/skills-ablation-20260908/verification-summary.json).
- All 179 source hashes in the completed supplemental v3 registration match the
  current files. No changed source, test, schema or dependency file was modified
  after the full suite. This closeout therefore reuses that full-suite result.
- Ruff, format checking, Pyright, TypeScript checking and all seven Node tests
  were rerun successfully during closeout.
- The first commit was exported independently and its provider/pi test selection
  completed with exit status zero. The second commit's Python source and test
  imports were checked for missing project modules; none were found. The final
  tree has the complete source and schema set covered by the full suite above.
- Public JSON artifacts parse successfully. Changed documentation links resolve.
  Text/field checks found no credentials, account identifiers, raw price rows or
  paid-news bodies in the public additions. Extractor text fixtures are synthetic;
  research data artifacts expose derived results, counts and content hashes.

No model inference, paid source request or broker call was made for Git closeout.
PVE remains shut down. Existing failed experiments and the unmerged prototype
branch are retained; cleanup is limited to the merged closeout branch and stale
worktree metadata.
