# Round-two evidence index

The owning report is [DL_SECOND_ROUND_20260908.md](../../DL_SECOND_ROUND_20260908.md). The bounded offline round is complete; the report separates confirmed numerical prediction from unvalidated news increment and execution. Public artifacts contain summaries, protocols and hashes; raw licensed observations, credentials, source bodies, model binaries and full research accounts remain private.

## Document model experiment

- [Protocol](protocol.md), [candidate screening](screening.md), [fixture manifest](fixture-manifest.json).
- [Frozen confirmation selection](confirmation-selection.json), [language results](language-results.json), [secondary format diagnosis](normalized-diagnostic.json).
- The issuer split and fixed answers precede inference. Development and confirmation are distinct. Formatting diagnostics do not overwrite the original scores.

## Numerical experiments

- [Method screening](method-screening.md) records the maintained baselines and newer HARLA, AlphaForge and AlphaPROBE alternatives, their evidence and the bounded execution choice.
- The report separates the public Qlib sample reproduction, the original small AlphaGen pilot, and the expanded private market capture. Their targets, universes, preprocessing and budgets differ.
- [Expanded protocol](expanded-numerical-protocol.md) owns nomination and conditional confirmation. Later clarifications and the OOM recovery are disclosed there.
- [Market audit](market-audit-summary.json), [functional verification](expanded-functional-verification.json), [cache verification](expanded-cache-verification.json), [pool geometry](expanded-pool-geometry-fixed.json), [compute selection](expanded-compute-selection.json).
- [Fresh edge-padding verification](fresh-padding-verification.json) checks the upstream supported six-session padding against 30-session padding on synthetic tensors; it opens no confirmation observations.
- [News readiness](news-increment-readiness.md) separates prospective evidence from a retrospective text pipeline demonstration.

## Completed results

- [Development summary](expanded-summary.json), [complete matrix audit](expanded-matrix-audit.json), [actual search budgets](expanded-search-audit.json).
- [Frozen nomination](fresh-nomination.json), [fresh confirmation](fresh-summary.json), [fresh artifact audit](fresh-artifact-audit.json).
- [GPU resource summary](gpu-resource-summary.json), [process cleanup](cleanup-audit.json).
- Nomination flags saying the holdout was unopened describe the state at nomination time; the fresh summary explicitly records that it was subsequently opened.

## Private reproduction sequence

[Environment versions](environment-versions.json) records the actual Python/package snapshots; [expanded failures](expanded-failures.json) preserves the adapter, resource and transfer recoveries.

Caller scripts and raw artifacts are retained in the workspace `.market-impact/dl-round2-20260908` and the corresponding isolated DL research directory. Frozen original adapter snapshots are also stored in each completed trial. Do not rerun commands blindly over existing trial directories: successful outputs are append-only, and failed attempts are preserved separately.

1. Replay and audit the captured Provider observations before building Qlib research binaries. Preserve actual receipt/hash and do not claim historical vendor vintage from a present-day capture.
2. Run `build_expanded_qlib.py`, then `expanded_features.py` using the training-only selected process count. The original feature generation ends at the purged 2024 development boundary.
3. Run the frozen `expanded_train.py` and `expanded_rl.py` trial matrix through `run_expanded_experiments.py`. The OOM-paused CPU lane was transferred to serial `recover_expanded_cpu.py`; the original controller remains responsible for GPU and account dispatch.
4. Run `expanded_size_diagnostic.py` and `expanded_backtest.py` on every result. Completion requires 42 original trials, 42 size checks and 84 cost accounts, not merely successful process transport or one controller's in-memory counter.
5. `summarize_expanded.py` refuses an incomplete matrix. Apply fixed nomination and paired differences without inspecting the fresh period.
6. `prepare_fresh_confirmation.py` refuses absent or duplicate nomination, freezes the summary hash and candidate, and creates adapters with the advanced temporal fold. `run_fresh_if_qualified.py` is the finite conditional continuation in the same experiment: it waits for all original evidence, runs confirmation only for the selected candidate, and does not choose a replacement if it fails.
7. Retain separate retrospective signal, upstream account, provider-vintage and native execution conclusions. An operational model/service or a live trading path is not part of this experiment.
