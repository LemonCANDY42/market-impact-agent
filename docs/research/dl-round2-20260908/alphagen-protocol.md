# Conditional AlphaGen pilot — frozen before search

The user's approved sequence calls for a small AlphaGen comparison if the numerical baseline reproduces. Qlib's two baselines and boundary/cost sensitivity completed positively; this activates that bounded research step.

- Pin upstream ICT-FinD-Lab/alphagen `259687e8f316994426416c530a94842a2fe6405e` (2026-06-04). Reuse its expression grammar, calculator, pool optimizer, Gym environment, LSTM feature extractor and SB3 MaskablePPO. No LLM/API assistance, trading agent, or production dependency.
- This is an OHLCV adaptation on the already acquired Qlib example data, **not reproduction of the paper's different Baostock dataset, date range or large search budget**. The current example data lack VWAP entirely. Load a calendar/feature/instrument rectangular tensor through upstream `StockData(preloaded_data=...)`; keep VWAP absent as NaN and mask only its feature token for both arms. Never replace it with close or fabricate turnover.
- Fixed seed 17; CSI300 historical membership, full available cross-section. Train 2008–2014, validation 2015–2016, historical test 2017–2020-08-01; purge the last 20 trading feature dates from train/validation. Target is the upstream 20-day forward return. Thus its IC is not directly comparable to Qlib's one-day label IC.
- Same upstream MSE pool capacity 10 and L1=0.005 for both arms. 4,096 masked expression-token actions per arm, with all completed expression attempts and elapsed time also reported. This matches action budget, not unique expression count or wall time. Random chooses uniformly from allowed actions. RL uses upstream two-layer 128-dimensional LSTM, gamma=1, entropy=0.01, batch=128; shorten rollout to 512 so this small budget includes eight rollout/training cycles (the maintained default is ten optimization epochs per cycle). Keep remaining maintained SB3 defaults and record dependency versions.
- One fixed 20-day reversal expression is a mechanical reference, chosen before evaluation. No test-driven factor sign, checkpoint, hyperparameter or pool choice. Finish both searches and save pools before opening validation/test scores. No early stopping based on held-out performance.
- CPU only, four Torch/BLAS threads; GPU remains allocated to the language comparison. No modified upstream source. Capture any compatibility failures. A failed run remains a failure, not a zero IC result.
- Outputs: actual steps, attempted/accepted expression counts, pool formulas and weights, train/validation/test IC and RankIC, daily-series artifacts, runtime cost. One seed and a small budget can show feasibility and directional evidence, not demonstrate general RL superiority or investment effectiveness. No account return claim.
