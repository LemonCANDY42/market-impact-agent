{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.market-impact-agent.local/prospective-supervisor-plan.schema.json",
  "title": "Market Impact Prospective Supervisor Plan",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "plan_id",
    "host_name",
    "host_uid",
    "launchd_label",
    "service_definition_path",
    "executable_path",
    "working_directory",
    "state_root",
    "environment_file",
    "stdout_path",
    "stderr_path",
    "invocation_interval_seconds",
    "maximum_state_bytes",
    "maximum_concurrent_opportunities",
    "notification_policy",
    "process_environment_isolation",
    "process_environment",
    "enabled_after_install",
    "execution_capability",
    "disabled_install_commands",
    "activation_commands",
    "rollback_commands"
  ],
  "properties": {
    "schema_version": {"const": "market-impact.prospective-supervisor-plan.v4"},
    "plan_id": {
      "type": "string",
      "pattern": "^prospective-supervisor-plan-[0-9a-f]{64}$"
    },
    "host_name": {"type": "string", "minLength": 1},
    "host_uid": {"type": "integer", "minimum": 1},
    "launchd_label": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9.-]+$"
    },
    "service_definition_path": {"type": "string", "pattern": "^/"},
    "executable_path": {"type": "string", "pattern": "^/"},
    "working_directory": {"type": "string", "pattern": "^/"},
    "state_root": {"type": "string", "pattern": "^/"},
    "environment_file": {"type": "string", "pattern": "^/"},
    "stdout_path": {"type": "string", "pattern": "^/"},
    "stderr_path": {"type": "string", "pattern": "^/"},
    "invocation_interval_seconds": {"type": "integer", "minimum": 10},
    "maximum_state_bytes": {"type": "integer", "minimum": 1},
    "maximum_concurrent_opportunities": {"type": "integer", "minimum": 1, "maximum": 16},
    "notification_policy": {"enum": ["health_log_only", "failed_runs_only"]},
    "process_environment_isolation": {"const": "clear_then_allowlist"},
    "process_environment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["PATH", "PYTHONUNBUFFERED"],
      "properties": {
        "PATH": {"const": "/usr/bin:/bin:/usr/sbin:/sbin"},
        "PYTHONUNBUFFERED": {"const": "1"}
      }
    },
    "enabled_after_install": {"const": false},
    "execution_capability": {"const": false},
    "disabled_install_commands": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "array",
        "minItems": 3,
        "items": {"type": "string", "minLength": 1}
      }
    },
    "activation_commands": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "array",
        "minItems": 3,
        "items": {"type": "string", "minLength": 1}
      }
    },
    "rollback_commands": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "prefixItems": [
        {
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "prefixItems": [
            {"const": "launchctl"},
            {"const": "bootout"},
            {"type": "string", "pattern": "^gui/[1-9][0-9]*$"},
            {"type": "string", "pattern": "^/.+\\.plist$"}
          ]
        },
        {
          "type": "array",
          "minItems": 3,
          "maxItems": 3,
          "prefixItems": [
            {"const": "launchctl"},
            {"const": "disable"},
            {
              "type": "string",
              "pattern": "^gui/[1-9][0-9]*/[A-Za-z0-9][A-Za-z0-9.-]+$"
            }
          ]
        },
        {
          "type": "array",
          "minItems": 3,
          "maxItems": 3,
          "prefixItems": [
            {"const": "/bin/rm"},
            {"const": "--"},
            {"type": "string", "pattern": "^/.+\\.plist$"}
          ]
        }
      ]
    }
  }
}
